global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "node-api"
    static_configs:
      - targets: ["node-api:4000"]

  - job_name: "python-forecasting"
    static_configs:
      - targets: ["python-forecasting:8000"]
