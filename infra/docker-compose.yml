version: '3.8'

services:
  postgres:
    image: timescale/timescaledb:latest-pg15
    container_name: inventoryai-postgres
    environment:
      POSTGRES_DB: inventoryai
      POSTGRES_USER: inventory_user
      POSTGRES_PASSWORD: inventory_password
    ports:
      - "5432:5432"
    volumes:
      - timescale_data:/var/lib/postgresql/data
      - ./db-migrations/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_initial_schema.sql
      - ./db-migrations/002_external_signals.sql:/docker-entrypoint-initdb.d/002_external_signals.sql
      - ./db-migrations/003_llm_recommendations.sql:/docker-entrypoint-initdb.d/003_llm_recommendations.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inventory_user -d inventoryai"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    container_name: inventoryai-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  prometheus:
    image: prom/prometheus:latest
    container_name: inventoryai-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/alerts.yml:/etc/prometheus/alerts.yml

  grafana:
    image: grafana/grafana:latest
    container_name: inventoryai-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    depends_on:
      - postgres
      - prometheus

  python-forecasting:
    build:
      context: ../forecasting-engine
      dockerfile: Dockerfile
    container_name: inventoryai-forecasting
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://inventory_user:inventory_password@postgres:5432/inventoryai
      REDIS_URL: redis://redis:6379/0
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENWEATHER_API_KEY: ${OPENWEATHER_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  node-api:
    build:
      context: ../backend-api
      dockerfile: Dockerfile
    container_name: inventoryai-node-api
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      DATABASE_URL: postgresql://inventory_user:inventory_password@postgres:5432/inventoryai
      FASTAPI_ENGINE_URL: http://python-forecasting:8000
      JWT_SECRET: super_secret_inventoryai_jwt_key_2026
    depends_on:
      postgres:
        condition: service_healthy
      python-forecasting:
        condition: service_started

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    container_name: inventoryai-frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - node-api

volumes:
  timescale_data:
