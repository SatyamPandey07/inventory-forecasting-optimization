{
  "annotations": { "list": [] },
  "editable": true,
  "id": null,
  "title": "5. System Health & Observability Dashboard",
  "tags": ["inventoryai", "system"],
  "timezone": "browser",
  "panels": [
    {
      "id": 1,
      "title": "API Latency (p50, p95, p99)",
      "type": "timeseries",
      "targets": [
        { "expr": "histogram_quantile(0.50, rate(api_latency_seconds_bucket[5m]))", "legendFormat": "p50 Latency" },
        { "expr": "histogram_quantile(0.95, rate(api_latency_seconds_bucket[5m]))", "legendFormat": "p95 Latency" },
        { "expr": "histogram_quantile(0.99, rate(api_latency_seconds_bucket[5m]))", "legendFormat": "p99 Latency" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 }
    },
    {
      "id": 2,
      "title": "Forecast Retraining Job Duration",
      "type": "timeseries",
      "targets": [{ "expr": "forecast_job_duration_seconds", "legendFormat": "Job Duration (s)" }],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 }
    }
  ],
  "schemaVersion": 38,
  "version": 1
}
